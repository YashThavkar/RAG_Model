[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "cedar-rag"
version = "0.1.0"
description = "CeDAR RAG — local app package"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["app*"]
